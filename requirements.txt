# ============================================
# نظام كنيسة الشهيدة دميانة - المكتبات المطلوبة
# متوافق مع Streamlit Cloud و Web Deployment
# ============================================

# --- إطار العمل الأساسي ---
streamlit==1.28.0

# --- التعامل مع جداول البيانات ---
pandas==2.1.2
openpyxl==3.1.2          # لقراءة/كتابة Excel

# --- Google APIs ---
gspread==5.12.0           # Google Sheets API
google-auth==2.23.4       # OAuth2 / Service Account
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.2.0

# --- الرسوم البيانية ---
plotly==5.18.0           # للرسوم التفاعلية

# --- الأمان ---
PyJWT==2.8.0             # JSON Web Tokens للمصادقة
cryptography==41.0.7     # تشفير إضافي

# --- HTTP Requests ---
requests==2.31.0         # لإرسال إشعارات Telegram

# --- QR Code ---
qrcode==8.0              # إنشاء QR Code للأعضاء
Pillow==10.1.0           # معالجة الصور

# --- Utilities ---
python-dateutil==2.8.2   # معالجة التواريخ
pytz==2023.3.post1       # المناطق الزمنية

# Optional: لحذف إذا لم تكن مستخدمة
# user-agents==2.3.0    # لتحديد معلومات المتصفح (اختياري)
